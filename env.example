# Copy to .env and fill in your API keys.
# Required for LLM: set at least one of the following.

# Google Gemini (used by default if GOOGLE_API_KEY is set)
GOOGLE_API_KEY=

# OpenAI (fallback if OPENAI_API_KEY is set and GOOGLE_API_KEY is not)
OPENAI_API_KEY=
